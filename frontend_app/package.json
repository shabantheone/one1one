{
  "name": "whatsapp-full-dp",
  "version": "1.0.0",
  "description": "WhatsApp Full DP - Android App",
  "main": "index.html",
  "scripts": {
    "build": "echo 'Building web assets...'",
    "serve": "npx http-server . -p 3000",
    "cap:add:android": "npx cap add android",
    "cap:sync": "npx cap sync",
    "cap:run:android": "npx cap run android",
    "cap:build:android": "npx cap build android",
    "android:dev": "npx cap run android --livereload --external",
    "android:build": "npm run build && npx cap sync && npx cap build android"
  },
  "dependencies": {
    "@capacitor/android": "^5.5.1",
    "@capacitor/camera": "^5.0.7",
    "@capacitor/core": "^5.5.1",
    "@capacitor/filesystem": "^5.1.4",
    "@capacitor/share": "^5.0.6",
    "@capacitor/status-bar": "^5.0.6",
    "@capacitor/splash-screen": "^5.0.6"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.5.1",
    "http-server": "^14.1.1"
  },
  "keywords": [
    "whatsapp",
    "profile-picture",
    "android",
    "capacitor",
    "image-processing"
  ],
  "author": "WhatsApp Full DP Team",
  "license": "MIT"
}